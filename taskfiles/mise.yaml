version: "3"

tasks:
  use:
    aliases: [u]
    cmds:
      - mise use {{.CLI_ARGS}}
    env:
      KERL_CONFIGURE_OPTIONS: --with-ssl={{.SSLOUT}} --with-ssl-incl={{.SSLDEV}} --with-odbc={{.ODBCOUT}}
      CPPFLAGS: -I{{.ODBCOUT}}/include
      LDFLAGS: -L{{.ODBCOUT}}/lib
